#!/usr/bin/env node

'use strict';

const { install } = require('elint');
const packageName = process.env.npm_package_name;
const packageNameRegexp = /^(@.+\/)?elint-preset-.+/;

if (packageName && packageNameRegexp.test(packageNameRegexp)) {
  install(packageName);
}
