const plugin = {
  name: 'elint-plugin-esm',
  title: 'esm',
  type: 'formatter',
  activateConfig: {
    extensions: ['.js']
  },
  async execute(text) {
    return {
      source: text,
      output: text,
      errorCount: 0,
      warningCount: 0
    }
  }
}

export default plugin